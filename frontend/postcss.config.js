/**
 * postcss.config.js
 * ------------------------------------------------------------------
 * Wires Tailwind + autoprefixer into the CSS build pipeline. You
 * should never need to touch this file.
 * ------------------------------------------------------------------
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
